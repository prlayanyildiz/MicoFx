MetaTrader5>=5.0.45
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
numpy>=1.26.0

# dev / test
pytest>=8.0.0
httpx>=0.28            # Starlette TestClient transport; 0.28 is what is installed
# The static checks pyproject.toml already configures. They were configured but
# never installed, so "python -m ruff" failed in a fresh venv and the checks got
# skipped by whoever hit that first - which is the same as not having them.
ruff>=0.6
mypy>=1.11

# Static checks (dev only - the bot does not import these).
# Run: python -m ruff check micofx/ tests/   -> must be clean
#      python -m mypy                        -> 28 known, see pyproject.toml
ruff>=0.16
mypy>=1.11
